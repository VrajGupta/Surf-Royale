# Handoff — T003 Deterministic Ocean Truth

## Completed

GitHub issue #24 / `tracker/technical-tickets/003-ocean-truth.md`.

- Added a server-seeded three-wave schedule with ordered, repeatable starts.
- Added CPU queries for height, normal, surface velocity, current, break phase, and underwater state.
- Added authored reef-break influence without any rendering dependency or GPU readback.
- Kept exact schedule data encapsulated; callers receive copies and normal client logs disclose nothing.

## Verification

```bash
./scripts/verify.sh ocean
./scripts/verify.sh bootstrap
```

Both pass. Ocean verification includes 10,000 headless samples under a 500 ms M4 ceiling.

## Red-team pass

Attacked seed divergence, external mutation of returned schedule data, finite/normalized outputs, surface-boundary classification, and CPU-query volume. No correctness failure survived. Added a schedule-copy isolation test and explicit M4 query-budget gate.

## Honest limitations

- The current model is a deterministic risk model, not final physical ocean simulation.
- Visual and audio swell cues consume `break_phase`, normals, and schedule-relative state in T006; this ticket intentionally contains no final renderer.
- Cross-machine deterministic tolerance will need evidence once a Windows artifact runs.

## Next ticket

T004 / GitHub #25 — momentum-honest six-state surf controller.
